load("@rules_python//python:defs.bzl", "py_library")

py_library(
    name = "conftest",
    srcs = ["conftest.py"],
)

load(":gentest.bzl", "gentest")

[
    gentest(f)
    for f in glob(["test_*.py"])
]
