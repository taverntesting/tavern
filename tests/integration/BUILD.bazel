load("@rules_python//python:defs.bzl", "py_library")

py_library(
    name = "conftest",
    srcs = [
        "conftest.py",
        "ext_functions.py",
        "server.py",
        "test_dummy.py",
    ],
    imports = ["../.."],
    visibility = ["//:__subpackages__"],
    deps = [
        "@tavern_pip//pypi__flask",
        "@tavern_pip//pypi__pyjwt",
        "@tavern_pip//pypi__pytest",
    ],
)

load(":gentest.bzl", "gentest")

[
    gentest(f)
    for f in glob(["test_*.tavern.yaml"])
]

load("@io_bazel_rules_docker//python3:image.bzl", "py3_image")

py3_image(
    name = "server_image",
    srcs = [":server.py"],
    base = "//bazelutil:py3_base_image",
    main = ":server.py",
    deps = [
        "@tavern_pip_flask//:pkg",
    ],
)
