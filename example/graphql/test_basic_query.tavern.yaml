---
test_name: GraphQL basic query - Get user by ID

includes:
  - !include graphql_config.yaml

stages:
  - name: Get user by ID
    graphql_request:
      url: "{graphql_server_url}/graphql"
      query: |
        query GetUser($id: ID!) {
          user(id: $id) {
            id
            name
            email
          }
        }
      variables:
        id: "1"
    graphql_response:
      json:
        data:
          user:
            id: "1"
            name: "John Doe"
            email: "john@example.com"

---
test_name: GraphQL basic query - Get all users

includes:
  - !include graphql_config.yaml

stages:
  - name: Get all users
    graphql_request:
      url: "{graphql_server_url}/graphql"
      query: |
        query GetUsers {
          users {
            id
            name
            email
          }
        }
    graphql_response:
      json:
        data:
          users:
            - id: "1"
              name: "John Doe"
              email: "john@example.com"
            - id: "2"
              name: "Jane Smith"
              email: "jane@example.com"

---
test_name: GraphQL basic query - Get posts

includes:
  - !include graphql_config.yaml

stages:
  - name: Get posts
    graphql_request:
      url: "{graphql_server_url}/graphql"
      query: |
        query GetPosts {
          posts {
            id
            title
            content
            authorId
          }
        }
    graphql_response:
      json:
        data:
          posts:
            - id: "1"
              title: "First Post"
              content: "Hello World!"
              authorId: "1"
            - id: "2"
              title: "Second Post"
              content: "GraphQL is cool"
              authorId: "2"